import React from "react";
import { data } from "../Pages/restApi.json";

const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
            Meet the passionate culinary artists and hospitality experts who work tirelessly to bring you an exceptional dining experience. From our master chefs with decades of experience to our warm and attentive service staff, every team member shares a common goal - making your time with us truly memorable.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;